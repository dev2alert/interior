import * as React from "react";
import Flex from "../../common/Main/Section/Flex";

export default function Reload(): React.ReactElement {
    return <Flex className="reload" justifyContent="center">
        <button className="button" />
    </Flex>;
}